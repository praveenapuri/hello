data "external" "efs_list" {
  program = [
    "bash",
    "-lc",
    <<-EOT
      set -euo pipefail
      aws efs describe-file-systems --region ${var.region} --output json \
      | jq -c '{ids: [.FileSystems[].FileSystemId]}'
    EOT
  ]
}
