# Get all Instance IDs and store them in a variable
$instanceIds = aws ec2 describe-instances --query "Reservations[].Instances[].InstanceId" --output text

# Update/Create the tag for those instances
aws ec2 create-tags --resources $instanceIds --tags Key=YourKey,Value=YourValue
