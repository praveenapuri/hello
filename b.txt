resource "aws_s3_bucket_lifecycle_configuration" "my_bucket_lifecycle" {
  bucket = aws_s3_bucket.my_bucket.id

  rule {
    id     = "log-storage-rule"
    status = "Enabled"

    expiration {
      days = 90 # You might be changing the days here
    }
  }
}
