# -----------------------------------------------------------------------------
# variables.tf
# -----------------------------------------------------------------------------
# This file defines the input variables for our configuration.
# -----------------------------------------------------------------------------

variable "domain_name" {
  description = "The domain name of the Route 53 hosted zone."
  type        = string
  default     = "epic-prod.aws.cignacloud.com"
}

variable "is_dr_active" {
  description = "Set to true to activate Disaster Recovery DNS records. Set to false for standard Production records."
  type        = bool
  default     = false # Default to production environment
}
