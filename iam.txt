 Error: Invalid function argument
│
│   on main.tf line 26, in resource "aws_route53_record" "a_records":
│   26:     for_each = lower(try(each.value.alias, "")) == "yes" ? [1] : []
│     ├────────────────
│     │ while calling lower(str)
│     │ each.value.alias is object with 6 attributes
│
│ Invalid value for "str" parameter: string required.
