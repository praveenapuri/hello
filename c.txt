approval_notification:
  stage: approval
  image: docker.cbp.dhs.gov/cloud/ubi/python:3.11
  needs: ["trigger_target_pipeline"]
  before_script:
    - 'curl -s -k -H "PRIVATE-TOKEN: $REBUILD_TOKEN" https://gitlab.cbp.dhs.gov/api/v4/projects/19048/repository/files/sendEmail.py/raw?ref=master -o sendEmail.py'
  script:
    - >
      python3 ./sendEmail.py
      -s "$EMAIL_FROM"
      -r "$EMAIL_TO"
      -c "$CR_NUM"
      -a "$APP_NAME"
      -ps "$START_TIME"
      -pd "$DURATION"
      -st "$CI_PIPELINE_CREATED_AT"
  rules:
    - when: always


test_result:
  stage: automationtests-at
  needs: ["trigger_target_pipeline"]
  script:
    - echo "TEST_STATUS=success" >> result.env
  artifacts:
    reports:
      dotenv: result.env
  rules:
    - when: on_success

test_result_failed:
  stage: automationtests-at
  needs: ["trigger_target_pipeline"]
  script:
    - echo "TEST_STATUS=failed" >> result.env
  artifacts:
    reports:
      dotenv: result.env
  rules:
    - when: on_failure




