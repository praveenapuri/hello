# modules/dashboards/outputs.tf

output "dashboard_names" {
  description = "A map of the names of all created CloudWatch dashboards."
  value = {
    for k, v in aws_cloudwatch_dashboard.ec2 : k => v.dashboard_name
    # When you add RDS dashboards, you would merge them in here:
    # merge(
    #   { for k, v in aws_cloudwatch_dashboard.ec2 : k => v.dashboard_name },
    #   { for k, v in aws_cloudwatch_dashboard.rds : k => v.dashboard_name }
    # )
  }
}

output "dashboard_arns" {
  description = "A map of the ARNs of all created CloudWatch dashboards."
  value = {
    for k, v in aws_cloudwatch_dashboard.ec2 : k => v.dashboard_arn
  }
}
