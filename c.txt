# 2. SNS Subscriptions for a list of email endpoints
resource "aws_sns_topic_subscription" "email_subscriptions" {
  for_each  = toset(var.notification_emails)
  topic_arn = aws_sns_topic.ssm_failure_notifications.arn
  protocol  = "email"
  endpoint  = each.value
}
